# Roster spreadsheets — contain real names, emails and safe-environment status.
# Never commit: git history is permanent and this repo may be shared.
*.xlsx
~$*.xlsx

# Local MCP / tooling config (may hold project refs or tokens)
.mcp.json
.claude/settings.local.json

# Netlify local state
.netlify/
deploy/.netlify/

# macOS cruft
.DS_Store
**/.DS_Store
